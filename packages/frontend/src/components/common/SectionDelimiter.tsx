// React
import { FC } from "react";
// SCSS
import delimiterStyles from "@/scss/components/common/SectionDelimiter.module.scss";

const SectionDelimiter: FC = () => {
  return <div className={delimiterStyles.delimiterContainer} />;
};

export default SectionDelimiter;
